---
- hosts: localhost
  become: yes
  gather_facts: yes

  tasks:
  - name: Umount all temporary mounts
    mount:
      path: "{{ item }}"
      state: unmounted
    with_items:
      - '/oldroot'
      - '/newroot/dev'
      - '/newroot/sys'
      - '/newroot/proc'
      - '/newroot/home'
      - '/newroot/tmp'
      - '/newroot/var/log/audit'
      - '/newroot/var/log'
      - '/newroot/var'
      - '/newroot/boot'
      - '/newroot'

  - name: Remove any temp files or directories
    file:
      path: "{{ item }}"
      state: absent
    with_items:
      - '/etc/fstab.fake'
      - '/etc/fstab.new'
      - '/newroot/newroot'
      - '/newroot/oldroot'       
      - '/newroot'
      - '/oldroot'
